User:
  type: object
  config:
    fields:
      id:
        type: "Identifier!"
      firstName:
        type: "String!"
        resolve: "@=value.getName().getFirstName()"
      lastName:
        type: "String!"
        resolve: "@=value.getName().getLastName()"
      clinic:
        type: "Clinic!"
